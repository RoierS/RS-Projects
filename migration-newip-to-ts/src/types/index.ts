export interface NewsAPIResponse {
  status: string;
  sourses: NewsSource[];
}

export interface NewsSource {
  id: string;
  name: string;
  description: string;
  url: string;
  category: string;
  language: string;
  country: string;
}
